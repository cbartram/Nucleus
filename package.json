{
  "name": "nucleus",
  "version": "1.0.0",
  "description": "Creates a new react component for you! ",
  "main": "./src/index.js",
  "bin": "./src/index.js",
  "scripts": {
    "package": "mkdir -p build && npx babel ./src/index.js --out-file ./build/main.js && pkg ./build/main.js --targets node6-macos --output ./bin/nucleus",
    "start": "node ./src/index.js Foo --dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "create",
    "react",
    "component",
    "frontend",
    "javascript"
  ],
  "author": "cbartram",
  "license": "ISC",
  "dependencies": {
    "chalk": "^2.4.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  },
  "babel": {
    "presets": [
      "env"
    ]
  }
}
